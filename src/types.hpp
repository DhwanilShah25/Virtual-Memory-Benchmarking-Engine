#pragma once

#ifndef TYPES_HPP
#define TYPES_HPP

#include <cstdint>

// Standardize our types across the entire engine
using PageID = uint32_t;
using FaultCount = uint32_t;
using FrameCount = uint32_t;

#endif